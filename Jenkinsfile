pipeline {
    agent any

    stages {
        stage('A') {
            steps {
                sh 'echo "state A"'
            }
        }
        stage('B') {
            steps {
                sh 'echo "state B"'
            }
        }
        stage('C') { 
            steps {
                sh 'echo "state C"'
            }
        }
    }
}
